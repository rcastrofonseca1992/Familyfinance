# Figma Make - Quick Start Card

## ⚡ 30-Second Setup

```typescript
// 1. Import what you need
import { DashboardPage, type Goal } from './app/exports';

// 2. Pass your data
<DashboardPage 
  goals={myGoals} 
  accounts={myAccounts} 
  debts={myDebts} 
/>

// 3. Done! 🎉
```

---

## 🎯 Common Tasks

### Use a Component

```typescript
import { GoalCard } from './app/components/GoalCard';

<GoalCard goal={goal} onClick={() => openGoal(goal)} />
```

### Use a Page

```typescript
import { PersonalFinancePage } from './app/pages/PersonalFinancePage';

<PersonalFinancePage
  accounts={accounts}
  debts={debts}
  incomes={incomes}
  fixedCosts={fixedCosts}
/>
```

### Use Types

```typescript
import type { Goal, Account, Debt } from './app/preview/types';

const myGoal: Goal = { /* ... */ };
```

### Use Mock Data

```typescript
import { mockGoals, mockAccounts } from './app/exports';

// Great for testing!
<GoalCard goal={mockGoals[0]} />
```

---

## 🛠️ Dev Commands

Open browser console in preview mode:

```javascript
devUtils.logDatabaseState()    // View all data
devUtils.getFinancialSummary()  // View summary
devUtils.addTestGoal()          // Add random goal
devUtils.resetDatabase()        // Reset everything
```

---

## 📁 File Locations

| What | Where |
|------|-------|
| Components | `/src/app/components/` |
| Pages | `/src/app/pages/` |
| Types | `/src/app/preview/types.ts` |
| Mock Data | `/src/app/preview/mock*.ts` |
| All Exports | `/src/app/exports.ts` |

---

## 🎨 Preview Mode

**Automatic Detection**
- Inside iframe → Preview mode with mocks
- Standalone → Production mode

**Manual Testing**
```html
<!-- Create preview-test.html -->
<iframe src="http://localhost:5173" width="100%" height="800"></iframe>
```

---

## 📚 Full Documentation

- **Quick Start**: `README.md` (this folder)
- **Complete Guide**: `USAGE_GUIDE.md` (30+ pages)
- **File Structure**: `STRUCTURE.md` (detailed map)

---

## 🚨 Important Rules

1. ✅ Import anything from `/src/app/`
2. ✅ Pass data as props (no globals)
3. ⚠️ Regenerating Make overwrites `/src/app/`
4. ✅ Copy custom changes outside this folder
5. ✅ Commit before regenerating

---

## 🔥 Pro Tips

### Tip 1: Import Everything at Once
```typescript
import {
  DashboardPage,
  GoalsPage,
  GoalCard,
  type Goal,
  mockGoals,
} from './app/exports';
```

### Tip 2: Use Mock Data for Tests
```typescript
import { mockGoals } from './app/exports';

describe('GoalCard', () => {
  it('renders', () => {
    render(<GoalCard goal={mockGoals[0]} />);
  });
});
```

### Tip 3: Dev Utils in Console
```javascript
// Available in preview mode
window.devUtils.addTestGoal();
window.devUtils.logDatabaseState();
```

### Tip 4: Type-Safe Integration
```typescript
import type { Goal } from './app/preview/types';

// Your Supabase data now type-checks!
const goals: Goal[] = await fetchGoalsFromSupabase();
```

---

## 🎯 Next Steps

1. ✅ Explore preview in Figma Make
2. ✅ Try dev commands in console
3. ✅ Import a component into your app
4. ✅ Read `USAGE_GUIDE.md` for details
5. ✅ Start building!

---

**Need Help?** Check `USAGE_GUIDE.md` for:
- Extending mock system
- Integration examples
- Troubleshooting
- Best practices

---

Generated by **Figma Make** | v1.0.0
