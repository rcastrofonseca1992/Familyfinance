STYLES LOCATION
===============

The main styles for this application are located at:
/styles/globals.css

This folder (/src/app/styles/) is reserved for any Make-specific style overrides.
The preview mode uses the same global styles as the production app.

DO NOT modify /styles/globals.css from Figma Make.
Only create additional stylesheets here if needed for preview-specific styling.
